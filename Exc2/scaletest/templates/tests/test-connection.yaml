apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaletest.fullname" . }}-test-connection"
  labels:
    {{- include "scaletest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scaletest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
