apiVersion: v1
kind: Service
metadata:
  name: {{ include "scaletest.fullname" . }}
  labels:
    {{- include "scaletest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scaletest.selectorLabels" . | nindent 4 }}
